import { HardhatRuntimeEnvironment } from "hardhat/types";
import { linkContracts } from "./link-contracts";

export async function deployAssetManager(hre: HardhatRuntimeEnvironment, configName: string) {
    
    // await linkContracts(hre, ["AssetManager"], `deployment/deploys/${configName}.libs.json`);
    
    
}
